Files created - input.txt
                patterns.sh
                vowels.txt
                consonats.txt
                mixed.txt

This script reads words from a text file and classifies them on the basis of vowel only, consonants only or a both vowels and consonants using loops and regular expressions. The file vowel.txt is empty since there is no word containing only vowels.
